import { ExamDetailPipe } from './exam-detail.pipe';

describe('ExamDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new ExamDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
