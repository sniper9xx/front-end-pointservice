import { ExamProductPipe } from './exam-product.pipe';

describe('ExamProductPipe', () => {
  it('create an instance', () => {
    const pipe = new ExamProductPipe();
    expect(pipe).toBeTruthy();
  });
});
