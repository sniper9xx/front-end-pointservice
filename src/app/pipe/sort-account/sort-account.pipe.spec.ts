import { SortAccountPipe } from './sort-account.pipe';

describe('SortAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
